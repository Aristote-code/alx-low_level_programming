#include <stdio.h>

/**
 * main - a program that prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

if('c' > 'a' && 'c' < 'z')
{
return (1);
}

else
{
return (0);
}

}
